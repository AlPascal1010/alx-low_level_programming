#include <stdio.h>
#include <stdlib.h>

/**
 * add - add 2 given integer
 * @a: numbers to add
 * @b: number to add
 * Return: sum of add
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub subtrats a nuber form anther
 * @a: number to be subtracted from
 * @b: number
 * Return: result of subtraction
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mul - multiplies two numbers
 * @a: number 1
 * @b: numberr 2
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - divide a number by another
 * @a: number
 * @b: numbers
 * Return: result of division
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 * mod - return the remmainder affter a division of numbers
 * @a: number
 * @b: number
 * Return: modulus of numbrs
 */
int mod(int a, int b)
{
	return (a % b);
}
