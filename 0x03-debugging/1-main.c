#include <stdio.h>

/**
 * main - causes infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incomming :(\n");

	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * potchar(i);
	 * }
	 */
	printf("infinite loop avoided! \\o/\n");

	return (0);
}
